<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.penley.myview" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.HideSomeActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".activities.DrawCircleActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".activities.StartViewActivity"
            android:label="@string/title_activity_start_view" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.AutoTypeActivity"
            android:label="@string/title_activity_auto_type" >
        </activity>
        <activity
            android:name=".activities.AwardActivity"
            android:label="@string/title_activity_award" >
        </activity>
    </application>

</manifest>
